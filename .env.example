# Shared app settings
DB_DRIVER=pgsql
DB_HOST=db
DB_PORT=5432
DB_NAME=bookdb
DB_USER=user
DB_PASS=password

# Optional base URL override
BASE_URL=http://localhost:8080/

# Dev-only Xdebug defaults
XDEBUG_MODE=off
XDEBUG_CONFIG=client_host=host.docker.internal client_port=9003
